export default function Footer() {
    return (
        <footer>
            <p>
            {/* %copy; {new_Date().getFullYear()} Michael Fisher */}
                footer
            </p>
        </footer>
    )
}